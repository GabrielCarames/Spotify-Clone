import useCategoryHelper from "../hooks/useCategoryHelper"

const Category = ({accessToken}) => {
    const {} = useCategoryHelper(accessToken)

    return (
        <div>hola</div>
    )
}

export default Category